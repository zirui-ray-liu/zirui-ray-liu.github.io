<style>
/* NeRF-project-page-inspired blog layout */
.nerf-page { max-width: 760px; margin: 1.5rem auto 4rem; }

.nerf-breadcrumb { font-size: .9rem; margin-bottom: 1.5rem; }
.nerf-breadcrumb a { color: var(--global-text-color-light); text-decoration: none; }
.nerf-breadcrumb a:hover { color: var(--global-theme-color); text-decoration: underline; }

.nerf-header { text-align: center; padding: 1rem 0 2rem; border-bottom: 1px solid var(--global-divider-color); margin-bottom: 2.5rem; }
.nerf-title { font-size: 3rem; font-weight: 700; margin: 0 0 .5rem; letter-spacing: -.02em; }
.nerf-subtitle { font-size: 1.25rem; font-weight: 400; color: var(--global-text-color); margin: 0 0 1.5rem; line-height: 1.4; }
.nerf-authors { font-size: 1rem; line-height: 1.7; margin: 0 0 .5rem; }
.nerf-authors a { color: var(--global-theme-color); text-decoration: none; }
.nerf-authors a:hover { text-decoration: underline; }
.nerf-authors sup { font-size: .75em; color: var(--global-text-color-light); }
.nerf-affils { font-size: .9rem; color: var(--global-text-color-light); margin: 0 0 .75rem; line-height: 1.6; }
.nerf-venue { font-size: .9rem; color: var(--global-text-color-light); margin: 0 0 1.5rem; font-style: italic; }

.nerf-buttons { display: flex; gap: .75rem; justify-content: center; flex-wrap: wrap; margin-top: 1.25rem; }
.nerf-btn {
  display: inline-flex; align-items: center; gap: .5rem;
  padding: .55rem 1.1rem;
  border: 1px solid var(--global-divider-color);
  border-radius: 999px;
  background: var(--global-bg-color);
  color: var(--global-text-color);
  font-size: .92rem;
  text-decoration: none;
  transition: background .15s ease, border-color .15s ease, color .15s ease;
}
.nerf-btn:hover { border-color: var(--global-theme-color); color: var(--global-theme-color); text-decoration: none; }
.nerf-btn i { font-size: 1rem; }

.nerf-hero { margin: 0 0 3rem; }
.nerf-hero figure { margin: 0; }
.nerf-hero-img { width: 100%; height: auto; border: 1px solid var(--global-divider-color); border-radius: 6px; background: #fff; }
.nerf-hero figcaption { margin-top: .85rem; font-size: .9rem; line-height: 1.6; color: var(--global-text-color-light); text-align: left; }

.nerf-section { margin: 2.5rem 0; }
.nerf-section h2 { font-size: 1.4rem; font-weight: 600; margin: 0 0 1rem; padding-bottom: .5rem; border-bottom: 1px solid var(--global-divider-color); }
.nerf-section p { font-size: 1rem; line-height: 1.75; margin: 0 0 1rem; }
.nerf-section ul, .nerf-section ol { line-height: 1.75; }

.nerf-bibtex {
  background: var(--global-code-bg-color, #f5f5f5);
  border: 1px solid var(--global-divider-color);
  border-radius: 4px;
  padding: 1rem 1.25rem;
  font-size: .88rem;
  line-height: 1.55;
  overflow-x: auto;
  white-space: pre;
}
</style>
